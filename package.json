{
  "name": "john-willis-static-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "npm run build && wrangler dev",
    "check": "astro check",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "overrides": {
    "yaml": "^2.9.0",
    "vite": "^7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.2",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.2",
    "typescript": "^5.9.3",
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0"
  }
}
